// @flow
import React from 'react';
import Timer from '../timer/timer';

const Layout = (props) => {
    return (
        <Timer 
            title="Eastern"
        />
    )
}

export default Layout;